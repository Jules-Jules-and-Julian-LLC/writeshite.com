<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.writinggame.db.mappers.Lobby">
    <resultMap id="lobbyResultMap" type="com.writinggame.model.Lobby">
        <id property="lobbyId" column="lobby_id"/>
        <result property="state" column="state" javaType="com.writinggame.domain.LobbyStateType"/>
        <result property="creatorUsername" column="creator_username"/>
        <result property="createDatetime" column="create_datetime" jdbcType="TIMESTAMP" javaType="java.time.OffsetDateTime"/>
        <result property="lastUpdateDatetime" column="last_update_datetime" jdbcType="TIMESTAMP" javaType="java.time.OffsetDateTime"/>
    </resultMap>

    <select id="find" resultMap="lobbyResultMap">
        SELECT *
        FROM lobby
        WHERE UPPER(lobby_id) = UPPEr(#{lobbyId})
    </select>

    <insert id="insert">
        INSERT INTO lobby(
            lobby_id,
            state,
            creator_username,
            create_datetime,
            last_update_datetime
        )
        VALUES (
            #{lobbyId},
            #{state},
            #{creatorUsername},
            #{createDatetime},
            #{lastUpdateDatetime}
        ) ON CONFLICT DO NOTHING
    </insert>

    <update id="update">
        UPDATE lobby
        SET lobby_id = #{lobbyId},
            state = #{state},
            creator_username = #{creatorUsername},
            create_datetime = #{createDatetime},
            lastUpdateDatetime = #{lastUpdateDatetime
        WHERE UPPER(lobby_id) = UPPER(#{lobbyId})
    </update>

    <delete id="delete">
        DELETE FROM lobby
        WHERE UPPER(lobby_id) = UPPER(#{lobbyId})
    </delete>

    <select id="findPlayerNames" resultType="String">
        SELECT player.username
        FROM player
        JOIN lobby ON lobby.lobby_id = player.lobby_id
        WHERE UPPER(lobby.lobby_id) = UPPER(#{lobbyId})
    </select>

</mapper>